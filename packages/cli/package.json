{
  "name": "@agent-bridge/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "agent-bridge": "dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-bridge/adapters": "workspace:*",
    "@agent-bridge/core": "workspace:*",
    "@agent-bridge/memory": "workspace:*",
    "commander": "^12.1.0"
  }
}
